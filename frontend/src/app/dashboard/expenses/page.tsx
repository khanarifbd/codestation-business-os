"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import {
  Ban,
  Download,
  Eye,
  FileText,
  FolderKanban,
  Loader2,
  Plus,
  Receipt,
  Store,
  Trash2,
  TrendingUp,
  Upload,
  X,
} from "lucide-react";
import { useRouter } from "next/navigation";
import { SearchableSelect } from "@/components/searchable-select";

type Tab = "expenses" | "vendors" | "categories" | "profitability";
type Vendor = { id:string; vendor_code:string; name:string; contact_name:string|null; email:string|null; phone:string|null; website:string|null; tax_identifier:string|null; country_code:string|null; currency:string|null; notes:string|null; is_active:boolean; created_at:string; updated_at:string };
type Category = { id:string; name:string; slug:string; cost_type:string; is_active:boolean; sort_order:number; created_at:string; updated_at:string };
type Account = { id:string; name:string; currency:string; current_balance:string|number; is_active:boolean };
type Client = { id:string; code:string; name:string; currency:string|null };
type Project = { id:string; number:string; name:string; order_id:string; client_id:string; client_name:string; currency:string; status:string };
type Order = { id:string; number:string; client_id:string; client_name:string; currency:string; total:string|number; status:string };
type Invoice = { id:string; number:string; client_id:string; client_name:string; order_id:string|null; project_id:string|null; currency:string; total:string|number; status:string };
type Payment = { id:string; number:string; invoice_id:string; invoice_number:string; client_id:string; client_name:string; invoice_currency:string; invoice_amount:string|number; payment_date:string; status:string };
type Meta = { vendors:Vendor[]; categories:Category[]; accounts:Account[]; clients:Client[]; projects:Project[]; orders:Order[]; invoices:Invoice[]; payments:Payment[] };
type CurrencySummary = { currency:string; posted_expenses:string|number; transfer_fees:string|number };
type Summary = { expense_count:number; posted_count:number; voided_count:number; vendor_count:number; receipt_count:number; project_expense_count:number; by_currency:CurrencySummary[] };
type ExpenseDoc = { id:string; expense_id:string; title:string; document_type:string; original_filename:string; content_type:string|null; size_bytes:number; notes:string|null; uploaded_by_user_id:string; created_at:string };
type ExpenseRow = { id:string; expense_number:string; description:string; expense_date:string; vendor_id:string|null; vendor_name:string|null; category_id:string; category_name:string; cost_type:string; account_id:string; account_name:string; client_id:string|null; client_name:string|null; project_id:string|null; project_number:string|null; project_name:string|null; order_id:string|null; order_number:string|null; invoice_id:string|null; invoice_number:string|null; payment_id:string|null; payment_number:string|null; expense_currency:string; expense_amount:string|number; account_currency:string; account_amount:string|number; exchange_rate:string|number; profitability_currency:string; profitability_amount:string|number; tax_amount:string|number; payment_method:string; reference:string|null; status:string; document_count:number; created_at:string };
type ExpenseDetail = ExpenseRow & { profitability_exchange_rate:string|number; notes:string|null; voided_at:string|null; documents:ExpenseDoc[] };
type Pnl = { currency:string; invoice_revenue:string|number; operating_expenses:string|number; transfer_fees:string|number; net_profit:string|number };
type ProjectProfit = { project_id:string; project_number:string; project_name:string; client_name:string; currency:string; contract_value:string|number; invoiced_revenue:string|number; collected_revenue:string|number; direct_expenses:string|number; estimated_profit:string|number; margin_percent:string|number|null };
type ClientProfit = { client_id:string; client_name:string; currency:string; invoiced_revenue:string|number; collected_revenue:string|number; direct_expenses:string|number; estimated_profit:string|number; margin_percent:string|number|null };
type Profitability = { profit_loss_by_currency:Pnl[]; projects:ProjectProfit[]; clients:ClientProfit[] };
type ExpensePage = { items:ExpenseRow[]; next_cursor:string|null };

type ExpenseForm = {
  description:string; category_id:string; account_id:string; vendor_id:string; client_id:string; project_id:string; order_id:string; invoice_id:string; payment_id:string;
  expense_date:string; expense_currency:string; expense_amount:string; account_amount:string; profitability_amount:string;
  tax_amount:string; payment_method:string; reference:string; notes:string;
};

const blankExpense:ExpenseForm = { description:"", category_id:"", account_id:"", vendor_id:"", client_id:"", project_id:"", order_id:"", invoice_id:"", payment_id:"", expense_date:"", expense_currency:"USD", expense_amount:"", account_amount:"", profitability_amount:"", tax_amount:"0", payment_method:"bank_transfer", reference:"", notes:"" };
const methods = ["bank_transfer","cash","card","payoneer","wise","stripe","paypal","fiverr","other"];
const costTypes = ["direct","operating","financial","tax","other"];

function pretty(value:string) { return value.replaceAll("_", " ").replace(/\b\w/g, (m) => m.toUpperCase()); }
function money(value:string|number, currency:string) { return `${currency} ${Number(value || 0).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}`; }
function fileSize(bytes:number) { if (bytes < 1024) return `${bytes} B`; if (bytes < 1024*1024) return `${(bytes/1024).toFixed(1)} KB`; return `${(bytes/1024/1024).toFixed(1)} MB`; }
function linkedTo(item:ExpenseRow) {
  const primary = item.payment_number || item.invoice_number || item.project_number || item.order_number || item.client_name || "Company";
  const secondary = [item.payment_number ? item.invoice_number : null, item.order_number, item.project_number].filter(Boolean).filter((value,index,array)=>array.indexOf(value)===index).join(" · ");
  return { primary, secondary };
}

export default function ExpensesPage() {
  const router = useRouter();
  const [tab,setTab] = useState<Tab>("expenses");
  const [summary,setSummary] = useState<Summary|null>(null);
  const [meta,setMeta] = useState<Meta>({vendors:[],categories:[],accounts:[],clients:[],projects:[],orders:[],invoices:[],payments:[]});
  const [expenses,setExpenses] = useState<ExpenseRow[]>([]);
  const [nextCursor,setNextCursor] = useState<string|null>(null);
  const [profitability,setProfitability] = useState<Profitability>({profit_loss_by_currency:[],projects:[],clients:[]});
  const [profitabilityLoaded,setProfitabilityLoaded] = useState(false);
  const [profitabilityLoading,setProfitabilityLoading] = useState(false);
  const [loading,setLoading] = useState(true);
  const [listLoading,setListLoading] = useState(false);
  const [loadingMore,setLoadingMore] = useState(false);
  const [saving,setSaving] = useState(false);
  const [error,setError] = useState<string|null>(null);
  const [message,setMessage] = useState<string|null>(null);
  const [modal,setModal] = useState<"expense"|"vendor"|"category"|"detail"|null>(null);
  const [selected,setSelected] = useState<ExpenseDetail|null>(null);
  const [expenseForm,setExpenseForm] = useState<ExpenseForm>(blankExpense);
  const [search,setSearch] = useState("");
  const [statusFilter,setStatusFilter] = useState("");
  const [categoryFilter,setCategoryFilter] = useState("");
  const [receiptFile,setReceiptFile] = useState<File|null>(null);
  const [receiptTitle,setReceiptTitle] = useState("");

  const api = useCallback(async (path:string, init?:RequestInit) => {
    const response = await fetch(`/api/finance${path}`, init);
    if (response.status === 401) { router.replace("/login"); throw new Error("Authentication required"); }
    const payload = response.status === 204 ? null : await response.json().catch(() => null);
    if (!response.ok) throw new Error(payload?.detail ?? "Expense request failed.");
    return payload;
  },[router]);

  const expenseQuery = useCallback((cursor?:string|null) => {
    const params=new URLSearchParams({limit:"50"});
    if(search.trim())params.set("search",search.trim());
    if(statusFilter)params.set("status",statusFilter);
    if(categoryFilter)params.set("category_id",categoryFilter);
    if(cursor)params.set("cursor",cursor);
    return params.toString();
  },[search,statusFilter,categoryFilter]);

  const refreshSummary = useCallback(async () => { setSummary(await api("/expense-summary") as Summary); },[api]);
  const refreshMeta = useCallback(async () => { setMeta(await api("/expense-meta") as Meta); },[api]);
  const refreshExpenses = useCallback(async (showLoader=false) => { if(showLoader)setListLoading(true);try{const data=await api(`/expenses?${expenseQuery()}`) as ExpensePage;setExpenses(data.items);setNextCursor(data.next_cursor);}finally{if(showLoader)setListLoading(false);}},[api,expenseQuery]);
  const refreshProfitability = useCallback(async () => { setProfitabilityLoading(true);try{setProfitability(await api("/profitability") as Profitability);setProfitabilityLoaded(true);}finally{setProfitabilityLoading(false);}},[api]);

  const bootstrap = useCallback(async () => {
    setLoading(true); setError(null);
    try {
      const [summaryData,metaData,expenseData] = await Promise.all([api("/expense-summary"),api("/expense-meta"),api(`/expenses?${new URLSearchParams({limit:"50"})}`)]);
      setSummary(summaryData as Summary); setMeta(metaData as Meta); const page=expenseData as ExpensePage; setExpenses(page.items); setNextCursor(page.next_cursor);
    } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to load expense workspace."); }
    finally { setLoading(false); }
  },[api]);

  useEffect(() => { void bootstrap(); },[bootstrap]);
  useEffect(() => { if(tab==="profitability" && !profitabilityLoaded && !profitabilityLoading) void refreshProfitability().catch((reason)=>setError(reason instanceof Error?reason.message:"Unable to load profitability.")); },[tab,profitabilityLoaded,profitabilityLoading,refreshProfitability]);

  const selectedAccount = useMemo(()=>meta.accounts.find((item)=>item.id===expenseForm.account_id)??null,[meta.accounts,expenseForm.account_id]);
  const selectedPayment = useMemo(()=>meta.payments.find((item)=>item.id===expenseForm.payment_id)??null,[meta.payments,expenseForm.payment_id]);
  const selectedInvoice = useMemo(()=>meta.invoices.find((item)=>item.id===(selectedPayment?.invoice_id || expenseForm.invoice_id))??null,[meta.invoices,selectedPayment,expenseForm.invoice_id]);
  const selectedProject = useMemo(()=>meta.projects.find((item)=>item.id===(selectedInvoice?.project_id || expenseForm.project_id))??null,[meta.projects,selectedInvoice,expenseForm.project_id]);
  const selectedOrder = useMemo(()=>meta.orders.find((item)=>item.id===(selectedProject?.order_id || selectedInvoice?.order_id || expenseForm.order_id))??null,[meta.orders,selectedProject,selectedInvoice,expenseForm.order_id]);
  const inheritedClientId = selectedProject?.client_id || selectedOrder?.client_id || selectedInvoice?.client_id || selectedPayment?.client_id || expenseForm.client_id;
  const selectedClient = useMemo(()=>meta.clients.find((item)=>item.id===inheritedClientId)??null,[meta.clients,inheritedClientId]);
  const profitabilityCurrency = selectedProject?.currency || selectedOrder?.currency || selectedInvoice?.currency || selectedClient?.currency || expenseForm.expense_currency;
  const accountFxNeeded = Boolean(selectedAccount && selectedAccount.currency !== expenseForm.expense_currency.toUpperCase());
  const profitFxNeeded = Boolean(profitabilityCurrency && profitabilityCurrency !== expenseForm.expense_currency.toUpperCase());

  const orderOptions = useMemo(()=>meta.orders.filter((item)=>!expenseForm.client_id || item.client_id===expenseForm.client_id).map((i)=>({value:i.id,label:`${i.number} · ${i.client_name} · ${money(i.total,i.currency)}`,keywords:`${i.number} ${i.client_name} ${i.currency} ${i.status}`})),[meta.orders,expenseForm.client_id]);
  const projectOptions = useMemo(()=>meta.projects.filter((item)=>!expenseForm.order_id || item.order_id===expenseForm.order_id).map((i)=>({value:i.id,label:`${i.number} · ${i.name} · ${i.currency}`,keywords:`${i.client_name} ${i.status}`})),[meta.projects,expenseForm.order_id]);
  const invoiceOptions = useMemo(()=>meta.invoices.filter((item)=>!expenseForm.order_id || !item.order_id || item.order_id===expenseForm.order_id).filter((item)=>!expenseForm.project_id || !item.project_id || item.project_id===expenseForm.project_id).map((i)=>({value:i.id,label:`${i.number} · ${i.client_name} · ${money(i.total,i.currency)} · ${pretty(i.status)}`,keywords:`${i.number} ${i.client_name} ${i.currency} ${i.status}`})),[meta.invoices,expenseForm.order_id,expenseForm.project_id]);
  const paymentOptions = useMemo(()=>meta.payments.filter((item)=>item.status==="confirmed").filter((item)=>!expenseForm.invoice_id || item.invoice_id===expenseForm.invoice_id).map((i)=>({value:i.id,label:`${i.number} · ${i.invoice_number} · ${money(i.invoice_amount,i.invoice_currency)} · ${i.payment_date}`,keywords:`${i.number} ${i.invoice_number} ${i.client_name}`})),[meta.payments,expenseForm.invoice_id]);

  function invalidateProfitability() { setProfitabilityLoaded(false); }
  function openExpense() { const account=meta.accounts.find((item)=>item.is_active);const category=meta.categories.find((item)=>item.is_active);setExpenseForm({...blankExpense,account_id:account?.id||"",category_id:category?.id||"",expense_currency:account?.currency||"USD"});setError(null);setModal("expense"); }

  function selectOrder(value:string) {
    const order=meta.orders.find((item)=>item.id===value);
    setExpenseForm((current)=>({...current,order_id:value,project_id:"",invoice_id:"",payment_id:"",client_id:order?"":current.client_id,profitability_amount:""}));
  }
  function selectProject(value:string) {
    const project=meta.projects.find((item)=>item.id===value);
    setExpenseForm((current)=>({...current,project_id:value,order_id:project?.order_id||current.order_id,invoice_id:"",payment_id:"",client_id:project?"":current.client_id,profitability_amount:""}));
  }
  function selectInvoice(value:string) {
    const invoice=meta.invoices.find((item)=>item.id===value);
    setExpenseForm((current)=>({...current,invoice_id:value,payment_id:"",order_id:invoice?.order_id||current.order_id,project_id:invoice?.project_id||current.project_id,client_id:invoice?"":current.client_id,profitability_amount:""}));
  }
  function selectPayment(value:string) {
    const payment=meta.payments.find((item)=>item.id===value); const invoice=meta.invoices.find((item)=>item.id===payment?.invoice_id);
    setExpenseForm((current)=>({...current,payment_id:value,invoice_id:payment?.invoice_id||current.invoice_id,order_id:invoice?.order_id||current.order_id,project_id:invoice?.project_id||current.project_id,client_id:payment?"":current.client_id,profitability_amount:""}));
  }

  async function submitExpense(event:React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); setSaving(true); setError(null);
    try {
      await api("/expenses",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({
        description:expenseForm.description,category_id:expenseForm.category_id,account_id:expenseForm.account_id,vendor_id:expenseForm.vendor_id||null,
        client_id:expenseForm.client_id||null,project_id:expenseForm.project_id||null,order_id:expenseForm.order_id||null,invoice_id:expenseForm.invoice_id||null,payment_id:expenseForm.payment_id||null,
        expense_date:expenseForm.expense_date||null,expense_currency:expenseForm.expense_currency.toUpperCase(),expense_amount:expenseForm.expense_amount,
        account_amount:accountFxNeeded?expenseForm.account_amount:null,profitability_amount:profitFxNeeded?expenseForm.profitability_amount:null,
        tax_amount:expenseForm.tax_amount||"0",payment_method:expenseForm.payment_method,reference:expenseForm.reference||null,notes:expenseForm.notes||null,
      })});
      setModal(null); setMessage("Expense posted, linked to the selected business records and account ledger updated."); invalidateProfitability();
      await Promise.all([refreshSummary(),refreshMeta(),refreshExpenses()]); if(tab==="profitability")await refreshProfitability();
    } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to post expense."); }
    finally { setSaving(false); }
  }

  async function openDetail(id:string) { setSaving(true);setError(null);try{const detail=await api(`/expenses/${id}`) as ExpenseDetail;setSelected(detail);setReceiptTitle(`${detail.expense_number} receipt`);setReceiptFile(null);setModal("detail");}catch(reason){setError(reason instanceof Error?reason.message:"Unable to open expense.");}finally{setSaving(false);} }
  async function voidExpense() { if(!selected||!window.confirm(`Void ${selected.expense_number}? The account ledger will be reversed.`))return;setSaving(true);setError(null);try{const detail=await api(`/expenses/${selected.id}/void`,{method:"POST"}) as ExpenseDetail;setSelected(detail);setMessage(`${detail.expense_number} voided and account balance restored.`);invalidateProfitability();await Promise.all([refreshSummary(),refreshMeta(),refreshExpenses()]);if(tab==="profitability")await refreshProfitability();}catch(reason){setError(reason instanceof Error?reason.message:"Unable to void expense.");}finally{setSaving(false);} }
  async function uploadReceipt(event:React.FormEvent<HTMLFormElement>) { event.preventDefault();if(!selected||!receiptFile)return;setSaving(true);setError(null);try{const data=new FormData();data.append("file",receiptFile);data.append("title",receiptTitle||receiptFile.name);data.append("document_type","receipt");const response=await fetch(`/api/expense-documents/${selected.id}/upload`,{method:"POST",body:data});const payload=await response.json().catch(()=>null);if(!response.ok)throw new Error(payload?.detail??"Unable to upload receipt.");const detail=await api(`/expenses/${selected.id}`) as ExpenseDetail;setSelected(detail);setReceiptFile(null);setReceiptTitle(`${detail.expense_number} receipt`);setMessage("Expense receipt uploaded.");await Promise.all([refreshSummary(),refreshExpenses()]);}catch(reason){setError(reason instanceof Error?reason.message:"Unable to upload receipt.");}finally{setSaving(false);} }
  async function deleteReceipt(documentId:string) { if(!selected||!window.confirm("Delete this receipt/document?"))return;setSaving(true);setError(null);try{const response=await fetch(`/api/expense-documents/${selected.id}/${documentId}`,{method:"DELETE"});const payload=response.status===204?null:await response.json().catch(()=>null);if(!response.ok)throw new Error(payload?.detail??"Unable to delete document.");const detail=await api(`/expenses/${selected.id}`) as ExpenseDetail;setSelected(detail);setMessage("Expense document deleted.");await Promise.all([refreshSummary(),refreshExpenses()]);}catch(reason){setError(reason instanceof Error?reason.message:"Unable to delete document.");}finally{setSaving(false);} }
  async function submitVendor(event:React.FormEvent<HTMLFormElement>) { event.preventDefault();const form=new FormData(event.currentTarget);setSaving(true);setError(null);try{await api("/vendors",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),contact_name:form.get("contact_name")||null,email:form.get("email")||null,phone:form.get("phone")||null,country_code:form.get("country_code")||null,currency:form.get("currency")||null,notes:form.get("notes")||null})});setModal(null);setMessage("Vendor created.");await Promise.all([refreshMeta(),refreshSummary()]);}catch(reason){setError(reason instanceof Error?reason.message:"Unable to create vendor.");}finally{setSaving(false);} }
  async function toggleVendor(item:Vendor) { setSaving(true);setError(null);try{await api(`/vendors/${item.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({is_active:!item.is_active})});await Promise.all([refreshMeta(),refreshSummary()]);}catch(reason){setError(reason instanceof Error?reason.message:"Unable to update vendor.");}finally{setSaving(false);} }
  async function submitCategory(event:React.FormEvent<HTMLFormElement>) { event.preventDefault();const form=new FormData(event.currentTarget);setSaving(true);setError(null);try{await api("/expense-categories",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),cost_type:form.get("cost_type"),sort_order:Number(form.get("sort_order")||0)})});setModal(null);setMessage("Expense category created.");await refreshMeta();}catch(reason){setError(reason instanceof Error?reason.message:"Unable to create category.");}finally{setSaving(false);} }
  async function toggleCategory(item:Category) { setSaving(true);setError(null);try{await api(`/expense-categories/${item.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({is_active:!item.is_active})});await refreshMeta();}catch(reason){setError(reason instanceof Error?reason.message:"Unable to update category.");}finally{setSaving(false);} }
  async function filterExpenses() { setError(null);try{await refreshExpenses(true);}catch(reason){setError(reason instanceof Error?reason.message:"Unable to filter expenses.");} }
  async function loadMore() { if(!nextCursor)return;setLoadingMore(true);setError(null);try{const data=await api(`/expenses?${expenseQuery(nextCursor)}`) as ExpensePage;setExpenses((current)=>[...current,...data.items]);setNextCursor(data.next_cursor);}catch(reason){setError(reason instanceof Error?reason.message:"Unable to load more expenses.");}finally{setLoadingMore(false);} }

  if (loading) return <main className="flex min-h-[70vh] items-center justify-center"><Loader2 className="size-7 animate-spin text-neutral-400"/></main>;

  return <main className="min-h-screen bg-neutral-100 p-5 sm:p-7 lg:p-9"><div className="mx-auto max-w-[1500px]">
    <div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-neutral-500">Costs, vendors, receipts and profitability</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Expenses</h1><p className="mt-2 max-w-3xl text-sm text-neutral-500">Posted expenses update the selected account ledger. Link costs to a client, order, project, invoice or payment when attribution matters.</p></div><div className="flex gap-2"><button onClick={()=>setModal("vendor")} className="rounded-xl border bg-white px-4 py-3 text-sm font-semibold">+ Vendor</button><button onClick={openExpense} className="rounded-xl bg-neutral-950 px-4 py-3 text-sm font-semibold text-white">+ New expense</button></div></div>
    <div className="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-6"><Metric label="Expenses" value={summary?.expense_count||0} icon={Receipt}/><Metric label="Posted" value={summary?.posted_count||0}/><Metric label="Voided" value={summary?.voided_count||0}/><Metric label="Vendors" value={summary?.vendor_count||0} icon={Store}/><Metric label="Receipts" value={summary?.receipt_count||0} icon={FileText}/><Metric label="Project costs" value={summary?.project_expense_count||0} icon={FolderKanban}/></div>
    {summary?.by_currency.length?<section className="mt-4 rounded-2xl border bg-white p-4 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wider text-neutral-400">Cost summary by currency</p><div className="mt-3 flex flex-wrap gap-3">{summary.by_currency.map((item)=><div key={item.currency} className="rounded-xl bg-neutral-50 px-4 py-3 text-sm"><span className="font-semibold">{item.currency}</span><span className="ml-3 text-neutral-500">Expenses {Number(item.posted_expenses).toLocaleString()} · Transfer fees {Number(item.transfer_fees).toLocaleString()}</span></div>)}</div></section>:null}
    {message?<div className="mt-4 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{message}</div>:null}
    {error && !modal?<div className="mt-4 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>:null}

    <div className="mt-5 rounded-2xl border bg-white p-2 shadow-sm"><div className="flex flex-wrap gap-1">{(["expenses","vendors","categories","profitability"] as Tab[]).map((item)=><button key={item} onClick={()=>setTab(item)} className={`rounded-xl px-4 py-2.5 text-sm font-medium ${tab===item?"bg-neutral-950 text-white":"text-neutral-500 hover:bg-neutral-100"}`}>{pretty(item)}</button>)}</div></div>

    {tab==="expenses"?<section className="mt-5 overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="flex flex-col gap-3 border-b p-4 lg:flex-row"><input value={search} onChange={(e)=>setSearch(e.target.value)} onKeyDown={(e)=>{if(e.key==="Enter")void filterExpenses();}} placeholder="Search expense, order, invoice, payment, reference..." className="h-11 flex-1 rounded-xl border px-3 text-sm"/><select value={statusFilter} onChange={(e)=>setStatusFilter(e.target.value)} className="h-11 rounded-xl border px-3 text-sm"><option value="">All statuses</option><option value="posted">Posted</option><option value="voided">Voided</option></select><select value={categoryFilter} onChange={(e)=>setCategoryFilter(e.target.value)} className="h-11 rounded-xl border px-3 text-sm"><option value="">All categories</option>{meta.categories.map((item)=><option key={item.id} value={item.id}>{item.name}</option>)}</select><button disabled={listLoading} onClick={()=>void filterExpenses()} className="h-11 rounded-xl border px-4 text-sm font-semibold disabled:opacity-50">{listLoading?"Applying...":"Apply"}</button></div><div className="overflow-x-auto"><table className="w-full min-w-[1200px] text-left text-sm"><thead className="border-b text-xs uppercase text-neutral-400"><tr><th className="p-4">Expense</th><th>Date</th><th>Vendor / Category</th><th>Linked to</th><th>Paid from</th><th>Expense</th><th>Account debit</th><th>Docs</th><th>Status</th><th className="pr-4 text-right">Action</th></tr></thead><tbody className="divide-y">{expenses.map((item)=>{const linked=linkedTo(item);return <tr key={item.id}><td className="p-4"><p className="font-semibold">{item.expense_number}</p><p className="mt-1 max-w-60 truncate text-xs text-neutral-400">{item.description}</p></td><td>{item.expense_date}</td><td><p>{item.vendor_name||"—"}</p><p className="text-xs text-neutral-400">{item.category_name}</p></td><td><p>{linked.primary}</p>{linked.secondary?<p className="text-xs text-neutral-400">{linked.secondary}</p>:null}</td><td>{item.account_name}</td><td>{money(item.expense_amount,item.expense_currency)}</td><td>{money(item.account_amount,item.account_currency)}</td><td>{item.document_count}</td><td><span className={`rounded-full px-2.5 py-1 text-xs ${item.status==="posted"?"bg-emerald-50 text-emerald-700":"bg-neutral-100 text-neutral-400"}`}>{pretty(item.status)}</span></td><td className="pr-4 text-right"><button onClick={()=>void openDetail(item.id)} className="rounded-lg border px-3 py-2 text-xs font-semibold">Open</button></td></tr>})}</tbody></table>{!expenses.length?<Empty text="No expenses found."/>:null}</div>{nextCursor?<div className="border-t p-4 text-center"><button disabled={loadingMore} onClick={()=>void loadMore()} className="rounded-xl border px-5 py-2.5 text-sm font-semibold disabled:opacity-50">{loadingMore?"Loading...":"Load more"}</button></div>:null}</section>:null}

    {tab==="vendors"?<section className="mt-5"><div className="mb-4 flex justify-end"><button onClick={()=>setModal("vendor")} className="inline-flex items-center gap-2 rounded-xl bg-neutral-950 px-4 py-3 text-sm font-semibold text-white"><Plus className="size-4"/>Add vendor</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{meta.vendors.map((item)=><article key={item.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-xs text-neutral-400">{item.vendor_code}{item.currency?` · ${item.currency}`:""}</p></div><span className={`rounded-full px-2.5 py-1 text-xs ${item.is_active?"bg-emerald-50 text-emerald-700":"bg-neutral-100 text-neutral-400"}`}>{item.is_active?"Active":"Inactive"}</span></div><div className="mt-4 space-y-1 text-sm text-neutral-500"><p>{item.contact_name||"No contact person"}</p><p>{item.email||"No email"}</p><p>{item.phone||"No phone"}</p></div><button disabled={saving} onClick={()=>void toggleVendor(item)} className="mt-4 w-full rounded-xl border px-3 py-2.5 text-sm font-semibold">{item.is_active?"Deactivate":"Activate"}</button></article>)}</div></section>:null}
    {tab==="categories"?<section className="mt-5 rounded-2xl border bg-white shadow-sm"><div className="flex items-center justify-between border-b p-5"><div><h2 className="font-semibold">Expense categories</h2><p className="mt-1 text-sm text-neutral-500">Built-in categories are editable and you can add your own.</p></div><button onClick={()=>setModal("category")} className="inline-flex items-center gap-2 rounded-xl bg-neutral-950 px-4 py-2.5 text-sm font-semibold text-white"><Plus className="size-4"/>Add category</button></div><div className="divide-y">{meta.categories.map((item)=><div key={item.id} className="flex items-center justify-between gap-4 p-4"><div><p className="font-medium">{item.name}</p><p className="mt-1 text-xs text-neutral-400">{pretty(item.cost_type)} · {item.slug}</p></div><div className="flex items-center gap-3"><span className={`rounded-full px-2.5 py-1 text-xs ${item.is_active?"bg-emerald-50 text-emerald-700":"bg-neutral-100 text-neutral-400"}`}>{item.is_active?"Active":"Inactive"}</span><button disabled={saving} onClick={()=>void toggleCategory(item)} className="rounded-lg border px-3 py-2 text-xs font-semibold">{item.is_active?"Disable":"Enable"}</button></div></div>)}</div></section>:null}
    {tab==="profitability"?(profitabilityLoading&&!profitabilityLoaded?<div className="mt-5 flex min-h-72 items-center justify-center rounded-2xl border bg-white"><Loader2 className="size-6 animate-spin text-neutral-400"/></div>:<section className="mt-5 space-y-5"><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-semibold">Profit & loss by currency</h2><p className="mt-1 text-sm text-neutral-500">Invoice revenue minus posted expenses and transfer fees. Currencies are never mixed.</p><div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{profitability.profit_loss_by_currency.map((item)=><div key={item.currency} className="rounded-xl bg-neutral-50 p-4"><div className="flex items-center justify-between"><p className="font-semibold">{item.currency}</p><TrendingUp className="size-4 text-neutral-400"/></div><p className="mt-4 text-2xl font-semibold">{money(item.net_profit,item.currency)}</p><div className="mt-3 space-y-1 text-xs text-neutral-500"><p>Revenue {money(item.invoice_revenue,item.currency)}</p><p>Expenses {money(item.operating_expenses,item.currency)}</p><p>Transfer fees {money(item.transfer_fees,item.currency)}</p></div></div>)}</div></div><ProfitTable title="Project profitability" rows={profitability.projects.map((item)=>({key:item.project_id,label:`${item.project_number} · ${item.project_name}`,sub:item.client_name,currency:item.currency,revenue:item.invoiced_revenue,collected:item.collected_revenue,cost:item.direct_expenses,profit:item.estimated_profit,margin:item.margin_percent}))}/><ProfitTable title="Client profitability" rows={profitability.clients.map((item)=>({key:`${item.client_id}-${item.currency}`,label:item.client_name,sub:item.currency,currency:item.currency,revenue:item.invoiced_revenue,collected:item.collected_revenue,cost:item.direct_expenses,profit:item.estimated_profit,margin:item.margin_percent}))}/></section>):null}
  </div>

  {modal==="expense"?<Modal title="Post expense" subtitle="Financial amounts are immutable after posting. Business relationships are recorded for traceability and reporting." onClose={()=>setModal(null)}><form onSubmit={submitExpense} className="space-y-4">{error?<ErrorBox text={error}/>:null}<div className="grid gap-4 sm:grid-cols-2"><Field label="Description" wide><input required value={expenseForm.description} onChange={(e)=>setExpenseForm({...expenseForm,description:e.target.value})} className="input" placeholder="Marketplace fee, hosting renewal, contractor payment..."/></Field><SearchableSelect label="Category" name="category_id" required value={expenseForm.category_id} onValueChange={(value)=>setExpenseForm({...expenseForm,category_id:value})} options={meta.categories.filter((i)=>i.is_active).map((i)=>({value:i.id,label:`${i.name} · ${pretty(i.cost_type)}`,keywords:i.slug}))} placeholder="Select category"/><SearchableSelect label="Vendor" name="vendor_id" value={expenseForm.vendor_id} onValueChange={(value)=>setExpenseForm({...expenseForm,vendor_id:value})} options={meta.vendors.filter((i)=>i.is_active).map((i)=>({value:i.id,label:`${i.vendor_code} · ${i.name}`,keywords:`${i.email||""} ${i.currency||""}`}))} placeholder="No vendor"/><SearchableSelect label="Paid from account" name="account_id" required value={expenseForm.account_id} onValueChange={(value)=>{const account=meta.accounts.find((i)=>i.id===value);setExpenseForm({...expenseForm,account_id:value,expense_currency:account?.currency||expenseForm.expense_currency,account_amount:""});}} options={meta.accounts.filter((i)=>i.is_active).map((i)=>({value:i.id,label:`${i.name} · ${i.currency} ${Number(i.current_balance).toFixed(2)}`,keywords:i.currency}))} placeholder="Select account"/><Field label="Expense date"><input type="date" value={expenseForm.expense_date} onChange={(e)=>setExpenseForm({...expenseForm,expense_date:e.target.value})} className="input"/></Field><Field label="Expense currency"><input required maxLength={3} value={expenseForm.expense_currency} onChange={(e)=>setExpenseForm({...expenseForm,expense_currency:e.target.value.toUpperCase()})} className="input uppercase"/></Field><Field label={`Expense amount (${expenseForm.expense_currency})`}><input required type="number" min="0.01" step="0.01" value={expenseForm.expense_amount} onChange={(e)=>setExpenseForm({...expenseForm,expense_amount:e.target.value})} className="input"/></Field>{accountFxNeeded?<Field label={`Actual debit from ${selectedAccount?.name} (${selectedAccount?.currency})`}><input required type="number" min="0.01" step="0.01" value={expenseForm.account_amount} onChange={(e)=>setExpenseForm({...expenseForm,account_amount:e.target.value})} className="input"/><small className="hint">Enter the exact amount deducted from the account.</small></Field>:null}
        <div className="sm:col-span-2 rounded-xl border bg-neutral-50 p-4"><p className="text-sm font-semibold">Business relationship (optional)</p><p className="mt-1 text-xs text-neutral-500">Link the cost to the most specific records you know. Business OS validates that all selected records belong to the same organization and business chain.</p><div className="mt-4 grid gap-4 sm:grid-cols-2"><SearchableSelect label="Client" name="client_id" value={expenseForm.client_id} onValueChange={(value)=>setExpenseForm({...expenseForm,client_id:value,order_id:"",project_id:"",invoice_id:"",payment_id:"",profitability_amount:""})} options={meta.clients.map((i)=>({value:i.id,label:`${i.code} · ${i.name}${i.currency?` · ${i.currency}`:""}`,keywords:i.currency||""}))} placeholder={selectedClient?`${selectedClient.name} (inherited)`:"Company expense"}/><SearchableSelect label="Order" name="order_id" value={expenseForm.order_id} onValueChange={selectOrder} options={orderOptions} placeholder={selectedOrder?`${selectedOrder.number} (inherited)`:"No order"}/><SearchableSelect label="Project" name="project_id" value={expenseForm.project_id} onValueChange={selectProject} options={projectOptions} placeholder={selectedProject?`${selectedProject.number} (inherited)`:"No project"}/><SearchableSelect label="Invoice" name="invoice_id" value={expenseForm.invoice_id} onValueChange={selectInvoice} options={invoiceOptions} placeholder={selectedInvoice?`${selectedInvoice.number} (inherited)`:"No invoice"}/><SearchableSelect label="Payment" name="payment_id" value={expenseForm.payment_id} onValueChange={selectPayment} options={paymentOptions} placeholder={selectedPayment?`${selectedPayment.number} (selected)`:"No payment"}/></div>{selectedClient?<p className="mt-3 text-xs text-neutral-500">Resolved client: <strong>{selectedClient.name}</strong>{selectedOrder?` · Order ${selectedOrder.number}`:""}{selectedInvoice?` · Invoice ${selectedInvoice.number}`:""}{selectedPayment?` · Payment ${selectedPayment.number}`:""}</p>:null}</div>
        {profitFxNeeded?<Field label={`Cost for profitability (${profitabilityCurrency})`}><input required type="number" min="0.01" step="0.01" value={expenseForm.profitability_amount} onChange={(e)=>setExpenseForm({...expenseForm,profitability_amount:e.target.value})} className="input"/><small className="hint">Normalize this expense to the linked project/order/client commercial currency.</small></Field>:null}<Field label={`Tax amount (${expenseForm.expense_currency})`}><input type="number" min="0" step="0.01" value={expenseForm.tax_amount} onChange={(e)=>setExpenseForm({...expenseForm,tax_amount:e.target.value})} className="input"/></Field><Field label="Payment method"><select value={expenseForm.payment_method} onChange={(e)=>setExpenseForm({...expenseForm,payment_method:e.target.value})} className="input">{methods.map((i)=><option key={i} value={i}>{pretty(i)}</option>)}</select></Field><Field label="Reference"><input value={expenseForm.reference} onChange={(e)=>setExpenseForm({...expenseForm,reference:e.target.value})} className="input" placeholder="Receipt / transaction ref"/></Field><Field label="Notes" wide><textarea rows={3} value={expenseForm.notes} onChange={(e)=>setExpenseForm({...expenseForm,notes:e.target.value})} className="textarea"/></Field></div><button disabled={saving} className="btn-primary w-full">{saving?"Posting...":"Post expense"}</button></form></Modal>:null}

  {modal==="vendor"?<Modal title="Add vendor" subtitle="Create suppliers, contractors, software providers or service vendors." onClose={()=>setModal(null)}><form onSubmit={submitVendor} className="space-y-4">{error?<ErrorBox text={error}/>:null}<div className="grid gap-4 sm:grid-cols-2"><Field label="Vendor name" wide><input name="name" required className="input"/></Field><Field label="Contact person"><input name="contact_name" className="input"/></Field><Field label="Email"><input name="email" type="email" className="input"/></Field><Field label="Phone"><input name="phone" className="input"/></Field><Field label="Country code"><input name="country_code" maxLength={2} placeholder="BD" className="input uppercase"/></Field><Field label="Default currency"><input name="currency" maxLength={3} placeholder="USD" className="input uppercase"/></Field><Field label="Notes" wide><textarea name="notes" rows={3} className="textarea"/></Field></div><button disabled={saving} className="btn-primary w-full">Create vendor</button></form></Modal>:null}
  {modal==="category"?<Modal title="Add expense category" subtitle="Categories can represent direct project costs, operating costs, financial charges or tax." onClose={()=>setModal(null)}><form onSubmit={submitCategory} className="space-y-4">{error?<ErrorBox text={error}/>:null}<Field label="Category name"><input name="name" required className="input"/></Field><Field label="Cost type"><select name="cost_type" defaultValue="operating" className="input">{costTypes.map((i)=><option key={i} value={i}>{pretty(i)}</option>)}</select></Field><Field label="Sort order"><input name="sort_order" type="number" min="0" defaultValue="500" className="input"/></Field><button disabled={saving} className="btn-primary w-full">Create category</button></form></Modal>:null}
  {modal==="detail"&&selected?<Modal title={`${selected.expense_number} · ${selected.description}`} subtitle={`${selected.expense_date} · ${selected.category_name}`} onClose={()=>setModal(null)} wide>{error?<ErrorBox text={error}/>:null}<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Info label="Expense" value={money(selected.expense_amount,selected.expense_currency)}/><Info label="Account debit" value={money(selected.account_amount,selected.account_currency)}/><Info label="Paid from" value={selected.account_name}/><Info label="Status" value={pretty(selected.status)}/><Info label="Vendor" value={selected.vendor_name||"—"}/><Info label="Client" value={selected.client_name||"Company"}/><Info label="Order" value={selected.order_number||"—"}/><Info label="Project" value={selected.project_number?`${selected.project_number} · ${selected.project_name}`:"—"}/><Info label="Invoice" value={selected.invoice_number||"—"}/><Info label="Payment" value={selected.payment_number||"—"}/><Info label="Profitability cost" value={money(selected.profitability_amount,selected.profitability_currency)}/><Info label="Tax" value={money(selected.tax_amount,selected.expense_currency)}/></div>{selected.notes?<div className="mt-4 rounded-xl bg-neutral-50 p-4 text-sm text-neutral-600">{selected.notes}</div>:null}<div className="mt-5 flex flex-wrap gap-2">{selected.status==="posted"?<button onClick={()=>void voidExpense()} disabled={saving} className="inline-flex items-center gap-2 rounded-xl border border-red-200 px-4 py-2.5 text-sm font-semibold text-red-700"><Ban className="size-4"/>Void expense</button>:null}</div><section className="mt-6 rounded-2xl border p-4"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Receipts & documents</h3><p className="mt-1 text-xs text-neutral-400">Private authenticated storage.</p></div><FileText className="size-5 text-neutral-300"/></div><form onSubmit={uploadReceipt} className="mt-4 grid gap-3 sm:grid-cols-[1fr_1.3fr_auto]"><input value={receiptTitle} onChange={(e)=>setReceiptTitle(e.target.value)} placeholder="Receipt title" className="input"/><input required type="file" onChange={(e)=>setReceiptFile(e.target.files?.[0]||null)} className="input py-2"/><button disabled={saving||!receiptFile} className="inline-flex items-center justify-center gap-2 rounded-xl bg-neutral-950 px-4 text-sm font-semibold text-white disabled:opacity-40"><Upload className="size-4"/>Upload</button></form><div className="mt-4 space-y-2">{selected.documents.map((doc)=><div key={doc.id} className="flex flex-col gap-3 rounded-xl bg-neutral-50 p-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-medium">{doc.title}</p><p className="mt-1 text-xs text-neutral-400">{doc.original_filename} · {fileSize(doc.size_bytes)} · {pretty(doc.document_type)}</p></div><div className="flex gap-2"><button onClick={()=>window.open(`/api/expense-documents/${selected.id}/${doc.id}/preview`,"_blank","noopener,noreferrer")} className="inline-flex items-center gap-1.5 rounded-lg border bg-white px-3 py-2 text-xs font-semibold"><Eye className="size-3.5"/>View</button><a href={`/api/expense-documents/${selected.id}/${doc.id}/file`} className="inline-flex items-center gap-1.5 rounded-lg border bg-white px-3 py-2 text-xs font-semibold"><Download className="size-3.5"/>Download</a><button onClick={()=>void deleteReceipt(doc.id)} className="inline-flex items-center gap-1.5 rounded-lg border border-red-100 bg-white px-3 py-2 text-xs font-semibold text-red-600"><Trash2 className="size-3.5"/>Delete</button></div></div>)}{!selected.documents.length?<p className="py-5 text-center text-sm text-neutral-400">No receipt uploaded yet.</p>:null}</div></section></Modal>:null}

  <style jsx global>{`.input{height:44px;width:100%;border:1px solid #e5e5e5;border-radius:12px;padding:0 12px;font-size:14px;font-weight:400;background:white}.textarea{width:100%;border:1px solid #e5e5e5;border-radius:12px;padding:10px 12px;font-size:14px;font-weight:400}.hint{display:block;margin-top:5px;font-size:11px;font-weight:400;color:#a3a3a3}.btn-primary{height:46px;border-radius:12px;background:#0a0a0a;color:white;font-size:14px;font-weight:600}.btn-primary:disabled{opacity:.5}`}</style>
</main>;
}

function Metric({label,value,icon:Icon}:{label:string;value:number;icon?:typeof Receipt}){return <div className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><p className="text-sm text-neutral-500">{label}</p>{Icon?<Icon className="size-4 text-neutral-400"/>:null}</div><p className="mt-3 text-2xl font-semibold">{value}</p></div>}
function Empty({text}:{text:string}){return <div className="flex min-h-52 items-center justify-center p-8 text-sm text-neutral-400">{text}</div>}
function ErrorBox({text}:{text:string}){return <div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{text}</div>}
function Field({label,children,wide=false}:{label:string;children:React.ReactNode;wide?:boolean}){return <label className={`text-sm font-medium ${wide?"sm:col-span-2":""}`}>{label}<div className="mt-2">{children}</div></label>}
function Info({label,value}:{label:string;value:string}){return <div className="rounded-xl bg-neutral-50 p-3"><p className="text-xs text-neutral-400">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></div>}
function Modal({title,subtitle,onClose,children,wide=false}:{title:string;subtitle?:string;onClose:()=>void;children:React.ReactNode;wide?:boolean}){return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/45 p-4"><div className={`max-h-[94vh] w-full overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl ${wide?"max-w-5xl":"max-w-3xl"}`}><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-semibold">{title}</h2>{subtitle?<p className="mt-1 text-sm text-neutral-500">{subtitle}</p>:null}</div><button onClick={onClose} className="rounded-lg p-2 hover:bg-neutral-100"><X className="size-5"/></button></div><div className="mt-5">{children}</div></div></div>}
function ProfitTable({title,rows}:{title:string;rows:{key:string;label:string;sub:string;currency:string;revenue:string|number;collected:string|number;cost:string|number;profit:string|number;margin:string|number|null}[]}){return <div className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-semibold">{title}</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b text-xs uppercase text-neutral-400"><tr><th className="p-4">Name</th><th>Revenue</th><th>Collected</th><th>Direct costs</th><th>Profit</th><th className="pr-4">Margin</th></tr></thead><tbody className="divide-y">{rows.map((row)=><tr key={row.key}><td className="p-4"><p className="font-semibold">{row.label}</p><p className="mt-1 text-xs text-neutral-400">{row.sub}</p></td><td>{money(row.revenue,row.currency)}</td><td>{money(row.collected,row.currency)}</td><td>{money(row.cost,row.currency)}</td><td className={Number(row.profit)>=0?"font-semibold text-emerald-700":"font-semibold text-red-700"}>{money(row.profit,row.currency)}</td><td className="pr-4">{row.margin===null?"—":`${Number(row.margin).toFixed(2)}%`}</td></tr>)}</tbody></table>{!rows.length?<Empty text="No profitability data yet."/>:null}</div></div>}
