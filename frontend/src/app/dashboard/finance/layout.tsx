import type { ReactNode } from "react";

// Finance navigation lives in the dashboard sidebar and mobile drawer.
export default function FinanceLayout({ children }: { children: ReactNode }) {
  return children;
}
